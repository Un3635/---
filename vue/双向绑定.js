/********************************************************
 * 
 * 双向绑定
 * 
 * 实现双向绑定的方法：
 * 1. 发布/订阅者模式
 * 2. 属性劫持
 * 3. 脏数据检测
 * 
 * 
 ********************************************************/
/***********************发布/订阅者******************************* */

function bind(src, k, v) {
  Object.defineProperty(src, k, {
    get() {
      return v;
    },
    set(nv) {
      if(nv === v) return;
      v = nv;
    }
  })
}
